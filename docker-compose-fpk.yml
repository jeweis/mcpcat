version: '3.8'

services:
  mcpcat:
    image: jeweis/mcpcat:latest
    container_name: mcpcat-app
    ports:
      - "${TRIM_SERVICE_PORT:-8000}:8000"
    environment:
      - APP_NAME=mcpcat
      - APP_VERSION=0.1.1
      - HOST=0.0.0.0
      - PORT=8000
      - LOG_LEVEL=INFO
      - MCPCAT_CONFIG_PATH=/app/.mcpcat/config.json
      - MCPCAT_DATABASE_PATH=/app/.mcpcat/mcpcat.db
      - MCPCAT_ARTIFACTS_PATH=/app/.mcpcat/skills/artifacts
      - MCPCAT_BACKUPS_PATH=/app/.mcpcat/backups
      - MCPCAT_SQLITE_BUSY_TIMEOUT_MS=5000
      - MCPCAT_SKILLS_ENABLED=${MCPCAT_SKILLS_ENABLED:-true}
    volumes:
      - mcpcat_config:/app/.mcpcat
    user: "1000:1000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT:-8000}/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  mcpcat_config:
